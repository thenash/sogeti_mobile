<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Connect.Views.ProjectInfoCell" xmlns:myViews="clr-namespace:Connect.Views;assembly=Connect">
    <Grid Margin="0,10,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <myViews:CardCellView x:Name="projectCodeCell" Grid.Row="0" Grid.Column="0" Title="Project Code:" Description="{Binding projectId}" TextAlignment="StartAndExpand" />
            <myViews:CardCellView x:Name="protocolIdCell" Grid.Row="0" Grid.Column="1" Title="Protocol ID:" Description="{Binding protocolId}" TextAlignment="EndAndExpand" />
            <myViews:CardCellView x:Name="customerNameCell" Grid.Row="1" Grid.Column="0" Title="Customer Name:" Description="{Binding customerName}" TextAlignment="StartAndExpand" />
            <myViews:CardCellView x:Name="businessUnitCell" Grid.Row="1" Grid.Column="1" Title="Business Unit:" Description="{Binding owningBu}" TextAlignment="EndAndExpand" />
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1" BackgroundColor="#F9B409">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Text=">" HorizontalOptions="Center" VerticalOptions="Center" FontAttributes="Bold"/>
        </Grid>
    </Grid>
</ViewCell>
